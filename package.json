{
  "name": "unclear",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that blocks CLEAR identity verification prompts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "pack": "node scripts/pack.js"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
